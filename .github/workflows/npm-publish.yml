name: CI & Publish

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  ci-scaffold-cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install, build, test
        working-directory: cli
        run: |
          npm ci
          npm run build
          npm test --if-present
      - name: Verify import
        working-directory: cli
        run: node -e "require('./dist/index.js')"
      - name: Check contents
        working-directory: cli
        run: npm pack --dry-run

  ci-react-vite-tagger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install, build, test
        working-directory: packages/react-vite-component-tagger
        run: |
          npm ci
          npm run build
          npm test --if-present
      - name: Verify import
        working-directory: packages/react-vite-component-tagger
        run: node -e "require('./dist/index.js')"
      - name: Check contents
        working-directory: packages/react-vite-component-tagger
        run: npm pack --dry-run

  ci-nextjs-webpack-tagger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install, build, test
        working-directory: packages/nextjs-webpack-component-tagger
        run: |
          npm ci
          npm run build
          npm test --if-present
      - name: Verify import
        working-directory: packages/nextjs-webpack-component-tagger
        run: node -e "require('./dist/index.js')"
      - name: Check contents
        working-directory: packages/nextjs-webpack-component-tagger
        run: npm pack --dry-run

  publish-scaffold-cli:
    needs: [ci-scaffold-cli]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      - working-directory: cli
        run: npm ci && npm run build
      - working-directory: cli
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-react-vite-tagger:
    needs: [ci-react-vite-tagger]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      - working-directory: packages/react-vite-component-tagger
        run: npm ci && npm run build
      - working-directory: packages/react-vite-component-tagger
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-nextjs-webpack-tagger:
    needs: [ci-nextjs-webpack-tagger]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      - working-directory: packages/nextjs-webpack-component-tagger
        run: npm ci && npm run build
      - working-directory: packages/nextjs-webpack-component-tagger
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
